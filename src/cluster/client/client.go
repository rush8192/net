package cluster/client

import "cluster"

CLUSTER_CMD_PORT = 8192

func create(key string, value []byte) string {
	return key
}

func update(key string, value []byte) string {
	return key
}

func delete(key string, value []byte) string {
	return key
}

func get(key string) string {
	
}

